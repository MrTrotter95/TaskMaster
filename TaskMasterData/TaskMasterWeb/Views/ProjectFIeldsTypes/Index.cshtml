@model IEnumerable<TaskMasterWeb.Models.ProjectFIeldsType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FieldTypeValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProjectField.FieldValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FieldTypeValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectField.FieldValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FieldTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FieldTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FieldTypeID })
        </td>
    </tr>
}

</table>
