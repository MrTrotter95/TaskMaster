@model IEnumerable<TaskMasterWeb.Models.AssignedProject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.ProjectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Staff.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.ProjectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Staff.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignedProjectsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignedProjectsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignedProjectsID })
        </td>
    </tr>
}

</table>
